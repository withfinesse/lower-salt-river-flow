[build]
  publish = "/"
  functions = "netlify/functions"

[[plugins]]
  package = "@netlify/plugin-functions-install-core"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["axios", "cheerio", "puppeteer"]

[functions.scrapeData]
  timeout = 30
